export default {
    // noWarningBusinessList: state => {
    //     state.businessList.map(item => {
    //         if (item.mini_pic.length === 0) {
    //             item.mini_pic[0] = ''
    //         }
    //         return item
    //     })
    // },
    state: state => state.businessList
}